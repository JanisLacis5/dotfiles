require("diffview").setup({
  enhanced_diff_hl = false,

  view = {
    default = {
      disable_diagnostics = true,
      winbar_info = true,
    },
    file_history = {
      disable_diagnostics = true,
      winbar_info = true,
    },
  },
})
